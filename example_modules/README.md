# Basic Commands
- node <file>
- node install underscore (This installs underscore module)
- node init (This builds the package.json file)
- node install backbone -S (This installs backbone and adds it to the package.json file)

# node_modules
- Do not include in repo. node install will do that.
